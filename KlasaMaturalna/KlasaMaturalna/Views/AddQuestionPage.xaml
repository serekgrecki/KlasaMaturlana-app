<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="KlasaMaturalna.Views.AddQuestionPage"
             xmlns:controls="clr-namespace:KlasaMaturalna.Controls"
             xmlns:ViewModel="clr-namespace:KlasaMaturalna.ViewModels;assembly=KlasaMaturalna"
             Title="Dodaj pytanie">
    <ContentPage.BindingContext>
        <ViewModel:AddQuestionViewModel/>
    </ContentPage.BindingContext>
    <ScrollView>
        <StackLayout>
            <Label Margin="20,20,20,0" HorizontalOptions="CenterAndExpand" Text="Jesteś już po egzaminie ustnym z języka polskiego? Podziel się pytaniem, które miałeś i pomoż pozostałym maturzystom!"/>
            <Label Margin="20,0,20,0" Text="Numer pytania:" HorizontalOptions="Start"/>
            <controls:MyPicker Margin="20,0,20,0"  ItemsSource="{Binding NumberQuestionsList}" SelectedIndex="{Binding IndexNumberQuestion}"/>
            <Label Margin="20,0,20,0" Text="Treść:" HorizontalOptions="Start"/>
            <Editor Margin="20,0,20,0" Text="{Binding QuestionContent}" VerticalOptions="FillAndExpand"/>
            <Label Margin="20,0,20,0" Text="Adres do obrazu:" HorizontalOptions="Start"/>
            <Editor Margin="20,0,20,0" Text="{Binding ImgSrc}"/>
            <Button Margin="20,0,20,0" Text="Wyślij!" FontSize="25" BackgroundColor="#5cb85c" Command="{Binding SendQuestionCommand}"/>
            <ActivityIndicator IsRunning="true" IsVisible="{Binding IsVisableLoading}" VerticalOptions="Center" HorizontalOptions="Center"/>
            <controls:AdMobView VerticalOptions="FillAndExpand"/>
        </StackLayout>
    </ScrollView>
</ContentPage>